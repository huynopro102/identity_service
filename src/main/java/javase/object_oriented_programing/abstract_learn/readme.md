# the concept of an abstract class : bất cứ lớp nào muốn nới rộng lớp abstract thì phải ghi đè lại tất cả các phương thức trừu tượng trong lớp abstract đó
## extends (nới rộng) , implements (triển khai)
## abstract có phương thức abstract và no abstract
## chỉ đc extends duy nhất 1 
## có thể implements nhiều
## interface dùng khi muốn chuẩn hóa hành vi
## abstract dùng khi muốn khái quát khóa hành vi